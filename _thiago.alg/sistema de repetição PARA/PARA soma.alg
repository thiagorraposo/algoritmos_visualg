algoritmo "escreva um algorítmo que leia a soma de 10 numeros quaisquer"
// Função : PARA
// Autor :
// Data : 27/09/2022
// Seção de Declarações 
var
   i, num, soma: inteiro
inicio
   i <- 1
   soma <- 0
   para i de 1 ate 10 faca
      escreva("digite um número: ")
      leia(num)
      soma <- soma + num
   fimpara
   escreva("a soma dos numeros é: ", soma)
fimalgoritmo
